# Copy to .env — use the SAME credentials that work in database/.env
# (do not use postgres:postgres unless that is your real Postgres password)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/url_shortener?schema=public"

PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:4200
